import 'package:flutter/material.dart';

class AppColors {
  // =========================================================
  // 🌞 LIGHT MODE
  // =========================================================
  static const Color primary = Color(0xFF1978E5);
  static const Color primaryLight = Color(0xFFE8F1FC);

  static const Color background = Color(0xFFF5F7F9);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceLow = Color(0xFFF8FAFC);

  static const Color border = Color(0xFFE2E8F0);

  static const Color textMain = Color(0xFF1E293B);
  static const Color textMuted = Color(0xFF64748B);

  // Extra UI
  static const Color divider = Color(0xFFE5E7EB);
  static const Color disabled = Color(0xFFCBD5F5);
  static const Color shadow = Color(0x1A000000);

  // =========================================================
  // 🌙 DARK MODE
  // =========================================================
  static const Color primaryDark = Color(0xFF044433);
  static const Color primaryDarkHeader = Color(0xFF0D4D3B);

  static const Color backgroundDark = Color(0xFF121212);
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color surfaceLowDark = Color(0xFF2A2A2A);

  static const Color borderDark = Color(0xFF2D2D2D);

  static const Color textMainDark = Color(0xFFFFFFFF);
  static const Color textMutedDark = Color(0xFF94A3B8);

  // Extra UI
  static const Color dividerDark = Color(0xFF2A2A2A);
  static const Color disabledDark = Color(0xFF3A3A3A);
  static const Color shadowDark = Color(0x66000000);

  // =========================================================
  // 🎨 COMMON ACCENTS
  // =========================================================
  static const Color accentGreen = Color(0xFF10B981);
  static const Color accentBlue = Color(0xFF3B82F6);
  static const Color accentOrange = Color(0xFFF59E0B);
  static const Color accentRed = Color(0xFFEF4444);
  static const Color accentPurple = Color(0xFF8B5CF6);

  // =========================================================
  // 📊 STATUS COLORS (UI dùng nhiều)
  // =========================================================
  static const Color success = Color(0xFF22C55E);
  static const Color info = Color(0xFF0EA5E9);
  static const Color warning = Color(0xFFFACC15);
  static const Color error = Color(0xFFDC2626);

  // =========================================================
  // 🎯 SPECIAL (gradient / brand)
  // =========================================================
  static const Color gradientStart = Color(0xFF044433);
  static const Color gradientEnd = Color(0xFF0D4D3B);
}